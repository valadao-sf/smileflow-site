# 2026-08-25 — Nath funnel route

## Shipped
- Nath Flow A at `smileflow.com.br/nath` — PARTIAL — [PR #8](https://github.com/valadao-sf/smileflow-site/pull/8) / https://smileflow.com.br/nath?source=instagram — the public route serves the approved local-only alpha and its three-audio recorder code; the physical microphone journey remains unverified because the controlled browser exposed no browser runtime.

## What went wrong
- The first prototype was published from `smileflow-mkt`; the active Vercel project belongs to `smileflow-site`, so the old root URL became a 404 when the canonical marketing runtime deployed.
- Grok spent two attempts reading and narrating for more than 60 seconds without writing; the port moved only after it was split into mechanical copy, import adaptation and route styling.
- The `smileflow-site` lockfile already reports three high-severity advisories on install; upgrading the framework was outside this route-only slice.

## Process fix proposed
- Before scaffolding a public acquisition page, inspect `origin/main` and the active deploy binding of every candidate repository; repository names and stale local checkouts are not ownership evidence.
